import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.lecorder.knoxweb',
  appName: 'ionicshake',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
